title: Journal
preset: journal
image:
  icon: 📓
  back: "#fff9e8"


sections:
  entries:
    type: pages
    label: Entries
    template: journal__entry
    info: "{{ page.date.toDate('d.m.Y') }}"
    empty: No entries yet
    sortBy: date desc
